#pragma once

namespace wm {
    enum class ButtonEvent {
        Pressed,
        Released
    };

    enum class Button {
        Left,
        Middle,
        Right,
        // scroll comes here too
    };
}